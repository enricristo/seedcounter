# Bancadas (C2) — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** abrir até quatro cenas ao mesmo tempo — quatro imagens para comparar, ou a mesma placa em quatro datas — cada uma com imagem, marcações, contornos, histórico, metadados e calibração próprios, com uma delas ativa.

**Architecture:** `useBancada()` compõe os hooks que **já são por-cena** (`useImageQueue`, `useMarks`, `useMetadata`, `useZoom`, `usePanning`) mais o estado de cena que hoje mora solto no `App.tsx` (imagem achatada, ajustes, máscara, contorno selecionado, região, contexto de dataset). O `App` passa a chamar `useBancada()` **quatro vezes, sempre** — hooks não podem ser chamados em laço condicional, e quatro instâncias de estado vazio não custam nada: o peso está nas imagens, e imagem só existe depois de carregada. `abertas` (1–4) decide quantas aparecem; `ativa` decide com quem a barra de ferramentas, os atalhos e o painel direito conversam.

**Tech Stack:** React 19, TypeScript com `strict` parcial (`strictNullChecks` + `noImplicitAny` ligados em 16/09), Dexie, Vitest (ambiente **node**, sem DOM e sem biblioteca de render).

**Spec:** `docs/superpowers/specs/2026-09-15-fila-bancadas-lote-eixos.md`, seção **C2** (desenho aprovado pelo Enrico em 16/09).

## Global Constraints

- **Comentários em português explicando o PORQUÊ.** Sem comentário que repete o código.
- **`src/lib/` nunca importa de `src/features/` nem de `src/components/`** (teste `src/lib/__tests__/dependencias.test.ts`).
- **Toda mutação de marca/contorno passa por `useMarks`** — agora, pelo `useMarks` **da bancada ativa**.
- **Nenhuma mudança de comportamento na Task 1.** É refatoração: os 949 testes, o `tsc` e o `build` são a rede; qualquer diferença visível é defeito.
- **Nenhuma dependência nova.** Não instale biblioteca de teste de componente para este plano; a garantia estrutural é teste estático de fonte, no idioma que o projeto já usa (`src/__tests__/corte-nunca-automatico.test.ts`).
- **Testes em ambiente node**: módulo de `src/lib/` não toca DOM, `localStorage` nem `Image`.
- **`strict` continua ligado.** Nada de `as any` nem `!` sem motivo escrito.
- **Ciano (`#00e5ff`) e magenta (`#ff3dc8`) só como legenda de viável/inviável**; o destaque da bancada ativa usa `--color-accent`.
- **Memória:** quatro digitalizações de 6800×9359 são ~1 GB de bitmap. A Task 4 é obrigatória antes de liberar 3–4 bancadas para uso real.
- **Antes de cada commit:** `npx vitest run` (949 na baseline), `npx tsc --noEmit`, `npx eslint src --ext .ts,.tsx` (0 erros), `npm run build`. Falhou, não commita.
- **Commit SEMPRE com pathspec explícito:** `git commit -m "..." -- <arquivos>`.
- **Mensagem de commit em português explicando o porquê**, terminando com `Co-Authored-By: Claude <noreply@anthropic.com>`.
- **Nunca escreva o nome da instituição** em código, comentário, documento ou mensagem de commit.

---

## Estado inicial (medido em 16/09)

- `src/App.tsx`: 3.672 linhas, ~140 hooks de topo.
- Já **por-cena** (prontos para entrar na bancada sem mudança): `useImageQueue`, `useMarks` (inclui histórico por gesto), `useMetadata`, `useZoom`, `usePanning`.
- Já **global** (ficam no App): `useTheme`, `useViewNavigation`, `useSessions`, `useExperiments`, `useFeatureFlags`, `useLaboratorio`, `useConta`, `usePerfisMedidos`, `useReceitasSalvas`, `useLotes`, `useDragDrop`, e todos os `is*Open` de modal.
- **Proto-bancada que já existe:** `anotacoesPorImagem` (ref, `Map<chave, {marks, yoloSegmentations}>`) guarda as anotações da imagem anterior ao navegar na fila. É a prova de que a cena já é uma unidade — a bancada só a torna explícita e simultânea.

## File Structure

| arquivo | responsabilidade |
|---|---|
| `src/hooks/useBancada.ts` *(novo)* | Uma cena: compõe os hooks por-cena e o estado de cena; devolve `Bancada`. |
| `src/hooks/useBancadas.ts` *(novo)* | Quatro instâncias fixas + `abertas` + `ativa` + `ativar`/`abrir`/`fechar`. |
| `src/features/bancadas/Bancadas.tsx` *(novo)* | Layout 1 / 2 / 2×2, borda da ativa, clique ativa. |
| `src/features/bancadas/comparacao.ts` *(novo, puro)* | Linhas de comparação entre bancadas e série no tempo. |
| `src/features/bancadas/__tests__/comparacao.test.ts` *(novo)* | Testes da comparação. |
| `src/__tests__/bancada-e-a-unidade-da-cena.test.ts` *(novo, estático)* | Garante que estado de cena não volta a nascer solto no `App.tsx`. |
| `src/App.tsx` | Passa a consumir a bancada ativa; perde o estado de cena. |
| `src/lib/db.ts` | `version(11)`: store `espacosDeTrabalho`. |

---

### Task 1: `useBancada()` — uma cena, com o comportamento de hoje

**Files:**
- Create: `src/hooks/useBancada.ts`
- Create: `src/__tests__/bancada-e-a-unidade-da-cena.test.ts`
- Modify: `src/App.tsx` (substitui as declarações por-cena por `const bancada = useBancada()` + desestruturação)

**Interfaces:**
- Consumes: `useImageQueue`, `useMarks`, `useMetadata`, `useZoom`, `usePanning` (assinaturas atuais, sem mudança).
- Produces:

```ts
export interface Bancada {
  id: string;
  /** Tudo de `useImageQueue` (image, filename, imageQueue, loadFiles, …). */
  fila: ReturnType<typeof useImageQueue>;
  /** Tudo de `useMarks` (marks, yoloSegmentations, desfazer, mutar, …). */
  anotacoes: ReturnType<typeof useMarks>;
  meta: ReturnType<typeof useMetadata>;
  zoom: ReturnType<typeof useZoom>;
  pan: ReturnType<typeof usePanning>;
  cena: EstadoDaCena;
}

export interface EstadoDaCena {
  fundoAchatado: HTMLImageElement | null;
  setFundoAchatado: (v: HTMLImageElement | null) => void;
  adjustments: ImageAdjustments;
  setAdjustments: React.Dispatch<React.SetStateAction<ImageAdjustments>>;
  adjustEnabled: boolean;
  setAdjustEnabled: React.Dispatch<React.SetStateAction<boolean>>;
  mascara: Mascara;
  setMascara: React.Dispatch<React.SetStateAction<Mascara>>;
  contornoSelecionado: number | null;
  setContornoSelecionado: React.Dispatch<React.SetStateAction<number | null>>;
  regiaoDeDeteccao: Regiao | null;
  setRegiaoDeDeteccao: React.Dispatch<React.SetStateAction<Regiao | null>>;
  ultimaGravacao: number | null;
  setUltimaGravacao: React.Dispatch<React.SetStateAction<number | null>>;
  forcarOriginalNasAutomacoes: boolean;
  setForcarOriginalNasAutomacoes: React.Dispatch<React.SetStateAction<boolean>>;
  /** Vínculo com o explorador de datasets, quando a imagem veio de lá. */
  anotacaoDoDataset: AnotacaoCarregada | null;
  setAnotacaoDoDataset: React.Dispatch<React.SetStateAction<AnotacaoCarregada | null>>;
  datasetContexto: { conjunto: string; caminho: string } | null;
  setDatasetContexto: React.Dispatch<React.SetStateAction<{ conjunto: string; caminho: string } | null>>;
  referenciaJaCarregada: boolean;
  setReferenciaJaCarregada: React.Dispatch<React.SetStateAction<boolean>>;
}

export function useBancada(id: string, opcoes?: { onImageLoaded?: (img: HTMLImageElement, file: File) => void }): Bancada;
```

**O que NÃO entra na bancada** (fica no App, é global): tema, vista, sessões, experimentos, flags, laboratório, conta, perfis medidos, receitas salvas, lotes, todos os `is*Open`, o painel direito (`rightSidebarTab`, recolhimentos), `mostrarEscala`, `mostrarEixos`, `ajusteDaMarca`, `showRulers`, `visualMode`, `activeClassification` — estes últimos são preferências de VISUALIZAÇÃO da pessoa, não da cena: quem liga a régua quer a régua nas quatro.

- [ ] **Step 1: Escrever o teste estático que falha**

`src/__tests__/bancada-e-a-unidade-da-cena.test.ts`:

```ts
import { describe, it, expect } from 'vitest';
import { readFileSync } from 'node:fs';
import { join } from 'node:path';

/**
 * A cena é uma UNIDADE. Este teste existe porque a tentação, sempre que
 * aparece um estado novo de imagem, é declará-lo no App "só desta vez" — e
 * foi assim que o App chegou a 3.672 linhas com a cena espalhada. Com quatro
 * bancadas, estado de cena solto no App vira estado COMPARTILHADO entre elas:
 * mexer numa mudaria a outra, e o defeito seria sutil.
 */
const APP = readFileSync(join(__dirname, '..', 'App.tsx'), 'utf8');
const BANCADA = readFileSync(join(__dirname, '..', 'hooks', 'useBancada.ts'), 'utf8');

const ESTADO_DE_CENA = [
  'fundoAchatado',
  'adjustments',
  'adjustEnabled',
  'mascara',
  'contornoSelecionado',
  'regiaoDeDeteccao',
  'ultimaGravacao',
  'forcarOriginalNasAutomacoes',
  'referenciaJaCarregada',
];

describe('a cena é uma unidade', () => {
  it('nenhum estado de cena é declarado direto no App', () => {
    for (const nome of ESTADO_DE_CENA) {
      const declaracao = new RegExp(`const \\[${nome},`);
      expect(APP, `${nome} precisa morar em useBancada, não no App`).not.toMatch(declaracao);
    }
  });

  it('todo estado de cena mora em useBancada', () => {
    for (const nome of ESTADO_DE_CENA) {
      expect(BANCADA, `${nome} faltando em useBancada`).toMatch(new RegExp(`const \\[${nome},`));
    }
  });

  it('os hooks por-cena são chamados dentro da bancada, não no App', () => {
    for (const hook of ['useImageQueue(', 'useMarks(', 'useMetadata(', 'useZoom(', 'usePanning(']) {
      expect(BANCADA, `${hook} precisa ser chamado em useBancada`).toContain(hook);
      expect(APP, `${hook} não pode ser chamado no App`).not.toContain(hook);
    }
  });
});
```

- [ ] **Step 2: Rodar e ver falhar**

`npx vitest run src/__tests__/bancada-e-a-unidade-da-cena.test.ts`
Esperado: FAIL — `useBancada.ts` não existe.

- [ ] **Step 3: Escrever `src/hooks/useBancada.ts`**

Compor os cinco hooks e declarar os nove estados de cena, **copiando os valores iniciais exatos do App** (ex.: `useState<Mascara>(MASCARA_INICIAL)`, `useState<ImageAdjustments>(NEUTRAL_ADJUSTMENTS)`, `useState(true)` para `adjustEnabled`). O cabeçalho do arquivo explica por que a cena é unidade e o que ficou de fora por ser preferência de visualização.

- [ ] **Step 4: Trocar no App**

`const bancada = useBancada('b1', { onImageLoaded });` e, logo abaixo, desestruturar com os MESMOS nomes que o App já usa, para o corpo do componente não mudar:

```ts
const { image, setImage, filename, setFilename, imageQueue, setImageQueue,
        currentImageIndex, setCurrentImageIndex, loadError, setLoadError,
        loadFiles, handleFileUpload, handleNextImage, handlePrevImage, loadImageFromFile } = bancada.fila;
const { marks, setMarks, yoloSegmentations, /* … */ mutar } = bancada.anotacoes;
const { metadata, setMetadata, updateMetadata } = bancada.meta;
const { zoomLevel, setZoomLevel, zoomIn, zoomOut, resetZoom, fitToScreen } = bancada.zoom;
const { isPanningMode, setIsPanningMode, startDrag, handleDrag, stopDrag, togglePanningMode } = bancada.pan;
const { fundoAchatado, setFundoAchatado, adjustments, setAdjustments, /* … */ } = bancada.cena;
```

Essa desestruturação é o que torna a Task 1 uma refatoração de verdade: **nenhuma outra linha do App muda**.

- [ ] **Step 5: Rodar tudo**

`npx vitest run` (949 + 3 novos = 952), `npx tsc --noEmit`, `npx eslint src --ext .ts,.tsx`, `npm run build`. Qualquer teste que passava e falhou é regressão: conserte antes de seguir.

- [ ] **Step 6: Commit**

```bash
git add src/hooks/useBancada.ts src/__tests__/bancada-e-a-unidade-da-cena.test.ts src/App.tsx
git commit -m "refactor(bancada): a cena vira uma unidade — useBancada()

Imagem, marcações, contornos, histórico, metadados, calibração e ajustes
sempre foram uma coisa só; estavam espalhados em ~40 declarações no App,
e com quatro bancadas isso viraria estado compartilhado entre elas. Nenhuma
mudança de comportamento: o App desestrutura com os mesmos nomes. Um teste
estático impede que estado de cena volte a nascer solto no App.

Co-Authored-By: Claude <noreply@anthropic.com>" -- src/hooks/useBancada.ts src/__tests__/bancada-e-a-unidade-da-cena.test.ts src/App.tsx
```

**Verificação humana (o Enrico faz):** abrir o preview e repetir o fluxo inteiro — carregar imagem, marcar, onda, editar contorno, desfazer/refazer, achatar fundo, ajustar canal, calibrar, exportar CSV e laudo, salvar sessão, navegar na fila (as anotações da imagem anterior continuam voltando). **Nada pode ter mudado.**

---

### Task 2: `useBancadas()` — quatro instâncias, uma ativa

**Files:**
- Create: `src/hooks/useBancadas.ts`
- Modify: `src/App.tsx` (troca `useBancada` por `useBancadas`; tudo passa a ler `ativa`)

**Interfaces:**
- Consumes: `useBancada` (Task 1).
- Produces:

```ts
export const MAXIMO_DE_BANCADAS = 4;

export interface Bancadas {
  /** Sempre quatro — hooks não podem ser chamados em laço condicional. */
  todas: Bancada[];
  /** Quantas aparecem na tela: 1 a 4. */
  abertas: number;
  /** Índice da ativa em `todas`. */
  indiceAtivo: number;
  ativa: Bancada;
  ativar(indice: number): void;
  /** Abre mais uma (até 4) e a torna ativa. Devolve o índice, ou null se já há quatro. */
  abrirNova(): number | null;
  /** Fecha uma bancada: zera o estado dela e reordena; nunca fecha a última. */
  fechar(indice: number): void;
}

export function useBancadas(opcoes?: { onImageLoaded?: (b: number, img: HTMLImageElement, file: File) => void }): Bancadas;
```

**Por que quatro sempre:** `useBancada()` chamado quatro vezes custa quatro conjuntos de `useState` vazios — barato. O que pesa é a imagem, e `image` é `null` até alguém carregar. A alternativa (um componente por bancada, com o hook dentro) é mais elegante e muito mais invasiva: exigiria mover metade do App para dentro do componente da cena. Fica registrada como o caminho da versão seguinte, se a comparação entre bancadas exigir.

- [ ] **Step 1: Escrever `useBancadas.ts`** — quatro `useBancada('b1'..'b4')`, `abertas`/`indiceAtivo` em estado, e `fechar` que chama `resetAllAnnotations()` + `setImage(null)` da bancada fechada antes de reordenar.

- [ ] **Step 2: No App**, trocar por `const bancadas = useBancadas({ onImageLoaded });` e `const bancada = bancadas.ativa;`. A desestruturação da Task 1 continua idêntica — **é por isso que a Task 1 vem primeiro**.

- [ ] **Step 3: Acrescentar ao teste estático** de `bancada-e-a-unidade-da-cena.test.ts`:

```ts
it('o App fala com a bancada ATIVA, não com uma bancada fixa', () => {
  expect(APP).toContain('bancadas.ativa');
  expect(APP).not.toMatch(/useBancada\(/);
});
```

- [ ] **Step 4:** `vitest`, `tsc`, `eslint`, `build`. **Step 5:** commit com pathspec.

---

### Task 3: Layout e a bancada ativa na tela

**Files:**
- Create: `src/features/bancadas/Bancadas.tsx`
- Modify: `src/App.tsx` (o viewport passa a ser `<Bancadas>`)
- Modify: `src/features/ajuda/atalhos.ts` (as teclas de bancada)

**Comportamento:**
- 1 bancada: idêntico a hoje. 2: lado a lado. 3 e 4: grade 2×2 (a terceira ocupa a célula vazia).
- A ativa tem borda `--color-accent` de 2 px e é a única que recebe atalhos, barra de ferramentas e painel direito. **Clicar em qualquer ponto de uma bancada a ativa** — inclusive um clique que também marca uma semente: ativar não pode custar um clique extra.
- Cabeçalho de 24 px por bancada: nome do arquivo (truncado), contagem, e um `×` para fechar (só aparece com mais de uma aberta).
- Teclas: `Ctrl+1..4` ativa a bancada N; `Ctrl+Shift+N` abre nova. Entram em `atalhos.ts` — o teste estático de atalhos exige.
- Bancada aberta e vazia mostra o `EmptyState` que já existe, com "Abrir imagem" — cada bancada carrega a sua.

- [ ] Steps: componente → ligação no App → atalhos → `vitest`/`tsc`/`eslint`/`build` → commit. Roteiro humano: abrir duas, carregar imagens diferentes, marcar numa e conferir que a outra não mudou, desfazer numa e conferir que não desfaz na outra.

---

### Task 4: Memória — bancada inativa não segura a imagem cheia

**Files:**
- Modify: `src/hooks/useBancada.ts` (guarda a versão reduzida e o `File` de origem)
- Modify: `src/features/bancadas/Bancadas.tsx` (pede a reduzida quando inativa)

**Por quê:** quatro digitalizações de 6800×9359 são ~1 GB de bitmap descomprimido. Um tablet cai.

**Regra:** ao sair de ativa, a bancada gera uma versão reduzida (maior lado ≤ 2000 px) para exibição e **libera o bitmap cheio**; ao voltar a ativa, recarrega a cheia do `File` original (que a fila já guarda). Anotações, histórico e metadados **não** são afetados — só pixels. Enquanto a cheia recarrega, a reduzida continua na tela: nada pisca.

- [ ] Steps: implementar → medir com quatro digitalizações reais (`performance.memory` quando existir, senão o tempo de troca) → registrar o número no progresso → commit.

---

### Task 5: Comparar — o que muda entre bancadas

**Files:**
- Create: `src/features/bancadas/comparacao.ts` (puro) + teste
- Modify: `src/components/layout/RightSidebar.tsx` (aba Resultados ganha o modo comparação)

```ts
export interface LinhaDeComparacao {
  rotulo: string;                    // nome do arquivo ou "D0", "D7"… quando há data
  bancada: number;
  total: number;
  viaveis: number;
  inviaveis: number;
  areaMedianaPx: number | null;
  areaMedianaMm2: number | null;     // null sem calibração — não inventa
  umPorPixel?: number;
}

/** Uma linha por bancada aberta com imagem. Ordem = ordem das bancadas. */
export function compararBancadas(entradas: EntradaDeComparacao[]): LinhaDeComparacao[];

/**
 * Série no tempo: só quando as bancadas são a MESMA placa (mesmo
 * `metadata.plate`) em datas diferentes. Devolve null quando não são — a
 * comparação lado a lado continua valendo, a série não.
 */
export function serieNoTempo(linhas: LinhaDeComparacao[], datas: (string | undefined)[]): SerieNoTempo | null;
```

**Regra que não cai:** comparar contagens de imagens com **calibrações diferentes** em mm² é erro; quando as escalas divergem mais de 5%, a tabela mostra px² e diz por quê.

- [ ] Steps: teste → função → painel → `vitest`/`tsc`/`eslint`/`build` → commit.

---

### Task 6: Espaço de trabalho que sobrevive a fechar a aba

**Files:** `src/lib/db.ts` (`version(11)`, store `espacosDeTrabalho: '++id, salvoEm'`), `src/hooks/useEspacoDeTrabalho.ts`, `Bancadas.tsx`.

Guarda quais sessões estão abertas em cada bancada, o layout e a ativa — **não** as imagens. Ao abrir o app, se havia um espaço salvo, oferece retomar. Mesmo padrão do lote (Dexie v10), que já provou o caminho.

- [ ] Steps: db → hook → banner de retomar → checagens → commit.

---

## Ordem, e o que é entregável sozinho

1. **Task 1** é entregável sozinha e **não muda nada para quem usa** — é a que compra a segurança de todas as outras.
2. **Tasks 2 + 3** entregam as bancadas de verdade (abrir, ativar, fechar).
3. **Task 4** é obrigatória antes de anunciar 3–4 bancadas.
4. **Tasks 5 e 6** são o que transforma bancada em ferramenta de acompanhamento no tempo.

## Checkpoint final

`npx vitest run` (949 + ~10), `npx tsc --noEmit`, `npx eslint src` (0 erros), `npm run build`, e o roteiro humano de cada task cumprido. `git push origin develop`. **Nunca em `main`.**

## Self-review

- **Cobertura da spec (C2):** (1) `Bancada` como unidade → Task 1; (2) 1–4 + ativa → Tasks 2 e 3; (3) ligação com longitudinal → Task 5; (4) memória → Task 4; (5) persistência → Task 6. Sincronizar zoom entre bancadas ficou **fora** — está na spec como opcional, e depende de as bancadas existirem para saber se vale; fica anotado no progresso.
- **Sem placeholders:** cada task tem arquivos exatos, assinaturas completas e o teste estático escrito por inteiro.
- **Consistência de tipos:** `Bancada` (Task 1) é o que `Bancadas.todas` carrega (Task 2), o que `Bancadas.tsx` renderiza (Task 3) e o que alimenta `EntradaDeComparacao` (Task 5). `EstadoDaCena` tem os nove nomes que o teste estático exige, e os mesmos nomes que o App desestrutura.
- **Risco declarado:** não há teste de render (sem biblioteca de componente, por decisão de não acrescentar dependência). A rede é: tipos com `strict`, 949 testes de lógica, teste estático de estrutura e o roteiro humano por task. Quem executar **precisa** dizer no relatório o que não conseguiu provar.

---

## Task 1 — feita (16/09, `118a620`)

952 testes, `tsc` limpo, eslint 0 erros, build ok. `App.tsx` 3.672 → 3.666 linhas (as ~40 declarações viraram uma chamada + desestruturação; o ganho líquido é pequeno porque entraram comentários explicando o que foi adiado — e isso é o certo).

**Adiado de propósito para a Task 2, com motivo:** as refs `marcasRef`, `segmentacoesRef`, `anotacoesPorImagem`, `chaveAtual` continuam no `App`, porque `onImageLoaded` — que as usa — precisa continuar global enquanto não recebe o índice da bancada.

**Nome que mudou em relação ao plano:** `anotacaoDoDataset` é, no código, `anotacaoAtual`. O código venceu o plano, como deve ser quando a diferença é só nome.

### ROTEIRO PARA O ENRICO — antes da Task 2

Esta é uma refatoração: **nada pode ter mudado**. Se qualquer passo abaixo se comportar diferente de ontem, é regressão e eu conserto antes de seguir.

1. Abrir uma imagem (exemplo real ou arquivo) — abre, ajusta o zoom sozinha.
2. Marcar com `V` e `I`; apagar com `Alt`; **desfazer/refazer** (`Ctrl+Z`, `Ctrl+Shift+Z`).
3. Onda (`S`) numa semente; ajustar um vértice (`C`); raspar por dentro.
4. Achatar fundo e depois **Preparar imagem** → trocar o canal (o canvas tem de mudar de verdade).
5. Calibrar (DPI ou régua) e conferir a **escala gráfica** no canto e o µm/px no rodapé.
6. Carregar 2–3 imagens na fila e **navegar entre elas**: as marcações de cada uma têm de voltar ao voltar.
7. Exportar CSV e laudo; salvar sessão e reabri-la pelo histórico.

**O que nenhum teste automatizado provou** (não há render neste projeto): exatamente os sete passos acima. É por isso que eles existem.

---

## Task 2 — feita (16/09, `30d0f18`)

955 testes (952 + 3 novos no teste estático), `tsc` limpo, eslint 0 erros, build ok.

**Além do plano:** as quatro refs adiadas na Task 1 (`marcasRef`, `segmentacoesRef`, `anotacoesPorImagem`, `chaveAtual`) migraram para dentro de `useBancada`. Cada bancada tem agora o próprio `Map` de cache e a própria chave atual; `onImageLoadedNaFila` (interno ao hook) salva a anotação que sai e carrega a que entra antes de repassar para o `onImageLoaded` que veio de fora. `onImageLoaded` do App agora recebe `(indice, img, file)` e resolve `alvo = bancadas.todas[indice] ?? bancadas.todas[0]` para `setMetadata`/`fitToScreen`/ensaio ao carregar — hoje `indice` é sempre 0 porque não há UI para trocar de bancada ativa (Task 3), então o comportamento com uma bancada aberta é idêntico ao de ontem.

`chaveAtual`, `marcasRef` e `segmentacoesRef` continuam expostas via `bancada.cena` porque o App ainda os lê fora do `onImageLoaded` (corte de contorno, borracha, arraste de vértice, `handleLoadSession`, o aviso de "sem semente marcada" do easter egg). `anotacoesPorImagem` e a função `chaveDaImagem` não precisaram sair de dentro do hook — nada no App as usava fora do fluxo que migrou.

**Efeito colateral limpo de brinde:** existiam DUAS cópias idênticas do `useEffect` que sincronizava `marcasRef`/`segmentacoesRef` com o estado (uma perto do topo do componente, outra perto do `useKeyboardShortcuts`). A migração absorveu as duas num só par de efeitos, dentro de `useBancada`.

**Descoberta que NÃO foi mexida, por estar fora do escopo das quatro refs:** `useMetadata` grava num registro ÚNICO do Dexie (`current_metadata`, chave fixa) — as quatro chamadas de `useMetadata()` dentro de `useBancadas` leem e escrevem o MESMO registro. Com uma bancada aberta isso não muda nada (só b1 é usada). Com quatro bancadas abertas e usadas de verdade, os metadados (pesquisador, projeto, tratamento, placa, DPI/µm-por-pixel, vínculo de dataset...) seriam GLOBAIS entre bancadas, não por-cena — o oposto do que a Task 1 e a Task 2 estão construindo para marcas/contornos/imagem. `fechar()` usa o `resetMetadata()` que o próprio hook já expõe, mas resetar reseta para TODAS as bancadas ao mesmo tempo (mesma chave). Isto precisa de decisão do Enrico antes da Task 3 dar suporte a múltiplas bancadas visíveis: ou `useMetadata` passa a aceitar uma chave por bancada (mudança de schema do Dexie, fora do escopo desta task), ou metadados viram deliberadamente globais na spec C2 (documentado, não acidental).

### ROTEIRO PARA O ENRICO — antes da Task 3

Mesma regra da Task 1: **nada pode ter mudado** com uma bancada aberta. Repita os sete passos do roteiro da Task 1 — a fila de imagens principalmente, é o ponto mais provável de regressão desta task (o cache de anotações por imagem trocou de dono).

**O que este agente não conseguiu provar** (não há teste de render neste projeto):

- Os sete passos do roteiro da Task 1, de novo — a lógica de cache mudou de lugar mesmo que o comportamento deva ser idêntico.
- `fechar()`, `abrirNova()` e `ativar()` não têm NENHUM caminho de UI ainda (Task 3) — só existem como funções do hook, exercitadas por raciocínio e pelos testes de tipo/estático, não por uso real. O comportamento de reordenar (`ordem`) ao fechar uma bancada do meio nunca rodou de verdade.
- A descoberta sobre `useMetadata` compartilhado acima é uma leitura de código, não uma reprodução — não há como reproduzir com uma bancada só.

---

## Task 3 — feita (16/09)

964 testes (958 + 6 novos: `resultado-por-bancada.test.ts` e um caso a mais em `atalhos.test.ts`), `tsc --noEmit` limpo, `eslint src` 0 erros (só os avisos preexistentes, nenhum deles em arquivo tocado por esta task), `npm run build` ok.

**O que entrou:**

- `src/features/bancadas/Bancadas.tsx` — layout 1/2/2×2. Com uma bancada aberta devolve `children` sem wrapper (`if (abertas <= 1) return <>{children}</>`): o layout de hoje não ganhou nem perdeu um pixel. Com duas ou mais, cada bancada vira uma célula de grade com cabeçalho de 24px (nome truncado + contagem + botão de fechar) e borda 2px (`border-accent` na ativa, `border-line` nas outras). O botão de fechar só aparece dentro do ramo de 2+ bancadas — que é sempre o caso ali dentro, já que o ramo de 1 bancada sai mais cedo.
- **Ativar por clique sem custar um clique extra:** `onClickCapture` no wrapper de cada célula chama `ativar(indice)` na fase de CAPTURA — antes do clique (fase de bolha) alcançar o botão "Procurar Arquivo" do `EmptyState` ou o canvas de marcação da ativa. Sem `stopPropagation`: o clique original sempre completa seu próprio efeito no mesmo gesto.
- **Só a ativa monta overlay caro.** As inativas usam `CenaInativa` (`React.memo`, props primitivas mais os dois arrays que só trocam de referência quando a bancada muda de verdade): reusa `ImageViewport` + `MarkingCanvas` para desenhar imagem, contornos e marcas, mas sem `contornoSelecionado`, sem `mostrarEixosDeTodos`, sem `showRulers`, sem `children` — `MarkingCanvas` já só monta o eixo de PCA, as réguas e o cursor da borracha quando o prop correspondente pede, então bastou não entregar o motivo. As sementes fantasma, o Germinar e a escala gráfica nem aparecem na árvore de uma inativa: só existem dentro do bloco `children`, que só a ativa recebe.
- **Automação amarrada à bancada errada, corrigida:** `onImageLoaded` (em `App.tsx`) já resolvia a bancada certa para zoom/metadados/dataset — mas o "ensaio ao carregar" (a Fase I por trás da flag) rodava para qualquer bancada que terminasse de carregar uma imagem, ativa ou não. Ganhou o guard "só se for a bancada ativa": carregar numa bancada fora de tela não dispara mais um ensaio caro que ninguém vai ver. O painel Encontrar, a medição de pasta e o lote já liam imagem/marcas/metadados vindos da bancada ATIVA — por construção, nunca tocavam bancada nenhuma além dela; não precisaram de mudança.
- **Worker de inferência, resultado × bancada que pediu:** o worker já cancelava a chamada anterior quando uma nova começa (a mais recente vence) — isso cobre trocar de bancada E pedir uma detecção nova nela. O buraco era trocar de bancada SEM pedir nada na nova: nada cancela a inferência da bancada antiga, e o resultado chegaria sozinho. O painel de IA ganhou uma prop com o id da bancada ativa no momento do render; o pedido de detecção captura esse id ANTES de esperar o resultado e, quando ele chega, compara com o id mais recente — descarta se a bancada mudou. Um segundo efeito zera a detecção pendente sempre que a bancada ativa muda, para "Confirmar" nunca aplicar à bancada nova um resultado ainda não confirmado da antiga.
- **Bancada fechada não renderiza nem decodifica:** `Bancadas.tsx` itera só as bancadas dentro de `abertas` — um slot fechado nunca aparece na árvore, então nunca passa pelo desenho do canvas. E a limpeza que fecha uma bancada (da Task 2) já zera a imagem, então não há bitmap para segurar mesmo se alguém tentasse.
- Teclas `Ctrl+1..4` (ativa a bancada N) e `Ctrl+Shift+N` (abre nova) em `useKeyboardShortcuts.ts`, tratadas num `if` dentro do bloco de Ctrl — com retorno antecipado, porque as teclas `1` e `2` sem Ctrl já são o modo de visualização, e as duas não podem disputar o mesmo caractere. Registradas em `atalhos.ts` (grupo novo "Bancadas") e cobertas por um caso novo em `atalhos.test.ts` (o teste existente só lê os `case` do gancho de teclado; como Ctrl+1..4 vive num `if`, precisou de um teste à parte).
- **Ponto de encaixe da Task 4**, comentado em `Bancadas.tsx` exatamente onde `CenaInativa` recebe a imagem da bancada: hoje é o bitmap cheio (Task 4 não é desta task); quando a Task 4 existir, troca pela versão reduzida que `useBancada` vai gerar ao a bancada sair de ativa.

**Teste novo:** `src/lib/__tests__/resultado-por-bancada.test.ts`, em node — simula a corrida com um cliente falso (uma promessa que resolve depois de um atraso, mesma forma do cliente real do worker): (1) a bancada troca ANTES da detecção terminar e sem pedir nada na nova, resultado descartado; (2) a bancada continua a mesma até o fim, aplicado; (3) quatro bancadas pedem quase juntas, reproduzindo o cancelamento que o cliente do worker já faz, só a mais recente aplica.

**Medição de troca entre bancadas — não medida de verdade.** A instrução era medir com duas bancadas abertas de digitalizações grandes; esta tarefa correu sem navegador (fora do escopo desta sessão) e por isso não há número cronometrado para registrar aqui. O raciocínio que existe, sem medição: hoje (antes da Task 4) trocar de bancada é só uma troca de índice — nenhum decode de imagem novo acontece, porque as duas já têm o bitmap cheio na memória o tempo todo; o custo é o de um re-render de React trocando qual bloco é a árvore "ativa" mais a troca de classe CSS das bordas. É exatamente por isso que a Task 4 é obrigatória antes de liberar 3–4 bancadas de digitalização grande para uso real: sem ela, quatro bancadas de 6800×9359 continuam as quatro na memória ao mesmo tempo, e trocar de ativa não abaixa esse custo nenhum. Quem for medir de verdade: abrir duas digitalizações grandes, alternar Ctrl+1/Ctrl+2 repetidamente com o profiler do navegador aberto, e registrar o tempo entre o keydown e o próximo paint.

### ROTEIRO PARA O ENRICO

1. Abrir o app com uma imagem — confirmar que nada mudou: sem cabeçalho, sem borda, viewport idêntico a ontem.
2. Ctrl+Shift+N para abrir uma segunda bancada — o layout vira lado a lado, cada metade com cabeçalho de 24px.
3. Carregar uma imagem DIFERENTE em cada bancada (clicar "Procurar Arquivo" na vazia — o clique já ativa e já abre o seletor, num gesto só).
4. Marcar algumas sementes na bancada 1; conferir que a bancada 2 (mostrando sua própria imagem, inativa) NÃO mudou — nem marca, nem contorno.
5. Ctrl+Z na bancada 1 (ativa); conferir que a bancada 2 continua intocada — o histórico é por bancada.
6. Alternar com Ctrl+1 / Ctrl+2 — a borda accent troca de lado; clicar diretamente dentro da célula da bancada 2 também deveria ativá-la (sem precisar da tecla).
7. Abrir uma terceira e uma quarta (grade 2×2); fechar uma pelo botão do cabeçalho — as restantes se reacomodam sem buraco na grade.
8. Fechar até sobrar uma: o botão de fechar deve sumir (não é permitido fechar a última) e o layout volta a ser o de uma bancada só.

**O que este agente não conseguiu provar** (sem navegador nesta sessão, e sem biblioteca de teste de componente no projeto):

- Todo o roteiro acima — layout, clique-ativa, e a borda de fato aparecendo na cor de destaque na tela.
- Que `React.memo` de fato evita o re-render das inativas na prática (a garantia aqui é de código — props primitivas e arrays estáveis —, não medida com o profiler do React).
- O tempo de alternar entre bancadas com digitalizações grandes — ver a seção acima.
- Que o painel de IA de fato descarta um resultado tardio dentro do app de verdade (o teste em `resultado-por-bancada.test.ts` prova a regra isolada, com um cliente falso; não substitui rodar uma detecção real, trocar de bancada no meio, e ver o resultado não aparecer).

---

## Task 3 — controle visível no cabeçalho (16/09, correção de bug relatado)

**Por quê:** o roteiro acima pedia Ctrl+Shift+N e Ctrl+1..4 — e o Enrico não conseguiu testar NADA da Task 3, porque o navegador e o sistema já são donos dessas combinações (aba anônima, trocar de aba, zoom). As bancadas existiam no código e eram invisíveis na tela: sem um único botão, ninguém sem saber os atalhos de cor descobria que elas existem.

**O que mudou:**

- `src/features/bancadas/SeletorDeBancadas.tsx` *(novo)* — o controle no cabeçalho, perto do chip de espécie (`Header.tsx`, linha 1). Com uma bancada aberta mostra só um `+` discreto (sem número, sem moldura — quem não usa bancadas não percebe que existem). Com duas ou mais: um botão por bancada, numerado, com o nome do arquivo truncado quando há um (`title` com o nome completo), a ativa em `bg-accent`/`text-accent-on` (a mesma linguagem de `--color-accent` do resto do produto), um `×` por bancada para fechar, e o `+` (some com quatro abertas). `aria-pressed` no botão da ativa, `aria-label` descritivo em cada botão.
- `src/components/layout/Header.tsx` — ganhou o slot `bancadasSlot`, no mesmo padrão de `especieSlot`/`contaSlot` (Header não conhece o tipo `Bancadas`, só recebe o `ReactNode` pronto).
- `src/hooks/useKeyboardShortcuts.ts` — os atalhos viraram **Ctrl+Alt+1..4** (ativar) e **Ctrl+Alt+N** (abrir nova), porque Ctrl sozinho e Ctrl+Shift já tinham dono no navegador/sistema; Ctrl+Alt não tem. Continuam existindo como atalho, mas deixaram de ser o ÚNICO caminho — o controle do cabeçalho é.
- `useTools.ts` (borracha temporária por Alt) **não precisou de mudança**: o guard `e.ctrlKey` já existia no topo do `onKeyDown` e já cobre Ctrl+Alt (quando Ctrl é pressionado antes ou junto do Alt, o que é o caso comum ao digitar um atalho de duas teclas). Achado durante esta correção: se alguém pressionar Alt ANTES de Ctrl (ordem invertida, incomum), a borracha temporária liga por um instante — comportamento pré-existente ao Alt sozinho, não introduzido por esta mudança, e não reproduzível sem navegador.
- `src/features/ajuda/atalhos.ts` e `atalhos.test.ts` — atualizados para Ctrl+Alt.

### ROTEIRO PARA O ENRICO (substitui o roteiro acima — agora sem depender de nenhum atalho)

1. Abrir o app com uma imagem — no cabeçalho, perto do chip de espécie, deve aparecer só um `+` pequeno e discreto. O resto da tela idêntico a ontem.
2. Clicar no `+` — abre a segunda bancada, o layout vira lado a lado, e o seletor no cabeçalho passa a mostrar dois botões numerados (1 e 2), o 1 destacado em `--color-accent` (é a ativa).
3. Carregar uma imagem em cada bancada — o nome do arquivo passa a aparecer truncado dentro do botão da bancada correspondente; passar o mouse sobre o botão mostra o nome completo.
4. Clicar no botão "2" do seletor — a bancada 2 fica ativa (borda accent troca de lado no viewport, e o botão 2 no cabeçalho fica destacado). Clicar diretamente dentro da célula da bancada 1 no viewport também deveria ativá-la de volta, sem precisar do seletor.
5. Clicar no `+` mais duas vezes — chegar a quatro bancadas; o `+` deve sumir do seletor (não dá para abrir uma quinta).
6. Clicar no `×` de uma bancada do meio — ela fecha, as restantes se reacomodam na grade, e o seletor no cabeçalho volta a mostrar só os números que sobraram (1, 2, 3), sem buraco.
7. Fechar até sobrar uma — o `×` deve sumir de todo botão (não é permitido fechar a última), e o seletor volta a mostrar só o `+` discreto.
8. Conferir que os atalhos antigos (Ctrl+1..4, Ctrl+Shift+N) NÃO fazem mais nada, e que Ctrl+Alt+1..4 e Ctrl+Alt+N continuam funcionando como atalho alternativo — sem que segurar Ctrl+Alt ligue a borracha por engano.
9. Junto do seletor: conferir os quatro pontos de desencaixe que motivaram esta rodada — controles de zoom não cortados pelo painel direito (recolhido e expandido), o controle deslizante da barra de ferramentas (V/I, contorno, borracha) sem estourar embaixo, a escala gráfica não capturando cliques do canvas fora do cabo/cantos/âncora, e o cabeçalho esquerdo (Histórico + desfazer/refazer/limpar) sem buraco visual.

**O que este agente não conseguiu provar** (sem navegador nesta sessão, e sem biblioteca de teste de componente no projeto):

- Todo o roteiro acima — o controle de fato aparecendo, o destaque de cor, o truncamento do nome, e os cinco pontos de desencaixe de fato corrigidos na tela.
- Que Ctrl+Alt+N/1..4 realmente escapam do sequestro do navegador/SO em todas as plataformas — só o raciocínio (Ctrl+Alt não é uma combinação de sistema documentada) sustenta a escolha.
- O caso de ordem invertida do Alt (Alt antes de Ctrl) citado acima, que já existia antes desta correção.

---

## Task 4 — feita (17/09)

987 testes (7 novos: `src/lib/__tests__/reduzir-imagem.test.ts`), `tsc --noEmit` limpo, `eslint src` 0 erros (74 avisos preexistentes, nenhum em arquivo tocado por esta task), `npm run build` ok. Esta sessão correu **sem navegador** e sem tocar `src/features/bancadas/comparacao.ts`, `src/components/layout/RightSidebar.tsx` nem `src/App.tsx` — arquivos de outro agente em paralelo (Task 5). Só `src/hooks/useBancada.ts`, `src/features/bancadas/Bancadas.tsx`, mais os dois arquivos novos de `src/lib/`, entraram no commit.

**O que entrou:**

- `src/lib/reduzir-imagem.ts` *(novo, puro)* — `deveReduzir(largura, altura)` (só reduz acima de `LADO_MINIMO_PARA_REDUZIR = 2500` no maior lado — regra 5: uma imagem de 640px não compensa) e `dimensoesReduzidas(largura, altura)` (maior lado ≤ `LADO_MAXIMO_REDUZIDO = 2000`, preservando a proporção). Puras e testadas em node (`src/lib/__tests__/reduzir-imagem.test.ts`, 7 casos): o piso, o teto, retrato vs. paisagem, e o caso degenerado de 1px.
- `src/hooks/useBancada.ts` — `cena.liberarImagemCheia()` e `cena.recarregarImagemCheia()`, mais o estado interno que as sustenta:
  - `arquivoPorChave` (`Map<chave, File>`) grava, dentro de `onImageLoadedNaFila`, o `File` que gerou CADA imagem que já passou pela bancada — a mesma chave (`nome:tamanho`) do cache de anotações que já existia da Task 2. Cobre navegação na fila, próxima/anterior E o recorte de contorno (`onCrop` → `loadImageFromFile(recorte)` também passa por `onImageLoadedNaFila`, então o crop vira a origem correta a recarregar, não a imagem pré-corte).
  - **Regra 4 sem sinal explícito de fora:** uma sessão restaurada zera `chaveAtual.current` (`App.handleLoadSession`, já existia antes desta task) sem passar pelo `onImageLoadedNaFila`. `arquivoDeOrigemAtual()` busca o `File` pela chave ATUAL — com `chaveAtual` nulo, não encontra nada, e `liberarImagemCheia` desiste sem precisar que ninguém lhe diga "esta bancada não tem File".
  - **A cheia é DE VERDADE liberada, não só trocada de nome.** A primeira versão desta task guardava a `HTMLImageElement` cheia numa ref "para recarregar rápido depois" — e um teste de raciocínio (não um teste automatizado; ver abaixo) mostrou que isso mantinha o bitmap de ~1GB vivo o tempo todo, exatamente o problema que a task existe para resolver. A versão corrigida guarda só um booleano (`estaReduzida`) e o `File` (leve); `recarregarImagemCheia` decodifica a cheia DE NOVO a partir do `File`, pagando o preço de tempo para não pagar o preço de memória.
  - **Regra 2 (geometria) sem tocar `MarkingCanvas`:** a reduzida ganha `.width`/`.height` FORÇADOS para o tamanho da cheia logo após gerada (`reduzida.width = imagemAtual.width`, idem `height`). `HTMLImageElement.width/height` são os valores DECLARADOS (o que todo o resto do código já lê — viewBox do SVG de marcas/contornos, tamanho do container em `MarkingCanvas`), diferentes de `naturalWidth/naturalHeight` (o tamanho real do bitmap). Como `MarkingCanvas` (`fundoEstatico`) desenha a imagem com `objectFit: 'fill'` dentro de um container do tamanho de `image.width × zoomLevel`, o `<img>` de baixa resolução estica para preencher exatamente o retângulo que a cheia ocuparia — borrado, mas geometricamente idêntico. Isto evitou mexer em `MarkingCanvas.tsx`, que não está entre os arquivos desta task.
  - **Corrida ativa↔inativa resolvida por INTENÇÃO, não por cancelamento.** `intencaoAtual` (`'ativa' | 'inativa'`) é escrita de forma síncrona no início de cada função; a promessa assíncrona (gerar a reduzida, ou decodificar a cheia) só aplica o resultado se `intencaoAtual` ainda for a mesma quando ela termina — senão descarta e revoga o blob. Alternar ativa/inativa rápido demais para a operação em voo terminar não aplica um resultado errado; só desperdiça o trabalho da operação que perdeu.
  - **Blob URL: quem cria não revoga.** `gerarImagemReduzida` cria a URL do blob e NÃO a revoga no próprio `onload` (diferente do padrão usado no decodificador TIFF de `useImageQueue.ts`) — porque `MarkingCanvas` lê `image.src` num `<img>` DOM próprio, separado do objeto `Image` usado para decodificar; revogar cedo quebraria esse `<img>` na primeira vez que tentasse carregar. Quem revoga é `recarregarImagemCheia`, depois que a reduzida deixa de ser referenciada por qualquer `<img>` na tela.
  - `decodificarImagemDeArquivo` e `gerarImagemReduzida` duplicam (de propósito) o decodificador TIFF/normal já existente em `useImageQueue.ts`: extrair um utilitário compartilhado tocaria um arquivo fora do escopo desta task, e os dois são pequenos o bastante para a duplicação sair mais barata que o risco de mexer num hook usado por todas as bancadas.
- `src/features/bancadas/Bancadas.tsx` — `GerenciadorDeMemoria`, um componente que não desenha nada (`return null`) e só observa `ativa` para chamar `recarregarImagemCheia()`/`liberarImagemCheia()`. Fica FORA do galho `ativa ? children : <CenaInativa />` de propósito: aquele galho desmonta um lado e monta o outro exatamente na troca, e um efeito que desmontasse nesse instante nunca disparia a chamada. Como as duas funções não são memoizadas com `useCallback` (o fechamento delas depende de `fila.image` atual, que muda a cada imagem carregada), o efeito roda mais vezes do que só nas transições reais de `ativa` — aceitável porque as duas são idempotentes (guardas internas por `ref`), e documentado no comentário do componente para não parecer descuido.
- O ponto de encaixe que a Task 3 deixou comentado em `Bancadas.tsx` foi substituído pela chamada real: `CenaInativa` continua recebendo `b.fila.image` sem mudança de assinatura — é o PRÓPRIO `useBancada` que já trocou o conteúdo desse campo pela reduzida.

**Medição — não medida de verdade, e a razão é mais específica que "sem navegador".** A tarefa pedia o tempo de (a) gerar a reduzida e (b) recarregar a cheia com duas imagens grandes. Além de não haver navegador nesta sessão (mesma limitação das Tasks 2 e 3), gerar uma imagem de 6800×9359 de verdade para medir exigiria ou uma digitalização real (não há uma neste ambiente) ou sintetizar um bitmap desse tamanho em memória só para o teste — o que mediria principalmente o custo de SINTETIZAR a imagem de teste, não o de reduzi-la. Nenhuma das duas frentes está disponível aqui. Roteiro para quem for medir com o app rodando:

1. Abrir duas digitalizações grandes (reais, de scanner) em duas bancadas.
2. No devtools, em `useBancada.ts`, envolver a chamada de `gerarImagemReduzida(...)` dentro de `liberarImagemCheia` com `performance.now()` antes e depois (no `.then`), e o mesmo em `recarregarImagemCheia` ao redor de `decodificarImagemDeArquivo(...)`. Logar a diferença.
3. Alternar Ctrl+Alt+1/Ctrl+Alt+2 (ou o seletor no cabeçalho) repetidamente; ler os tempos no console.
4. Registrar aqui: tempo de gerar a reduzida (deve ser dominado pelo `drawImage` do canvas, proporcional aos pixels da CHEIA, não da reduzida) e tempo de recarregar a cheia (deve ser dominado pela decodificação do `File` — TIFF é mais lento que PNG/JPEG, ver `decodificarTiff`).
5. Also medir com `performance.memory` (Chrome, atrás de `--enable-precise-memory-info` ou já exposto em alguns builds) antes e depois de reduzir todas as bancadas inativas, para confirmar que o heap realmente cai — é a prova final de que a Task 4 cumpriu o propósito, não só a geometria.

**O que ficou de fora, com motivo:**

- **Nenhum teste de render** (mesma limitação estrutural das Tasks 1–3: sem biblioteca de teste de componente, por decisão do plano). Os 7 testes novos cobrem só a parte PURA (`deveReduzir`/`dimensoesReduzidas`); a geração real do bitmap (canvas, `Image`, blob) não roda em nenhum teste — só em navegador.
- **`arquivoPorChave` cresce sem limite** ao longo da vida de uma bancada (um `File` por imagem já vista), do mesmo jeito que `anotacoesPorImagem` (Task 1/2) já fazia. `File` é leve (metadado + referência ao disco, não o bitmap decodificado), então o custo é pequeno comparado ao problema que a task resolve — mas fica registrado como o mesmo padrão, não um padrão novo.
- **Fechar uma bancada (`zerarBancada`, Task 2) não reseta `estaReduzida`/`reduzindoOuRecarregando` explicitamente** — não precisou: `zerarBancada` já zera `fila.image` e `chaveAtual`, e qualquer chamada futura a `liberar`/`recarregar` já retorna cedo por causa disso; o próximo carregamento de imagem de verdade (`onImageLoadedNaFila`) reseta `estaReduzida` de qualquer forma. Não foi reproduzido em navegador — é leitura de código.
- **Erro ao recarregar a cheia** (arquivo indisponível, corrompido) cai num `console.error` em vez de um aviso na interface: uma bancada inativa não tem barra de erro própria (`loadError` de `CenaInativa` é só leitura, sem UI de retry). Registrado como limitação conhecida, não como bug — o comportamento seguro (ficar com a reduzida em vez de travar) está garantido.

### ROTEIRO PARA O ENRICO

1. Abrir duas digitalizações GRANDES (as maiores que tiver à mão) em duas bancadas.
2. Alternar entre elas (clique na célula ou Ctrl+Alt+1/2): a bancada que sai de vista deve, depois de um instante, mostrar uma versão um pouco mais borrada da mesma imagem (a reduzida) — sem sumir, sem piscar para branco.
3. Voltar para essa bancada: por um instante pode continuar borrada (a cheia decodificando de novo), depois nitidez total volta — sem a imagem pular de posição ou de tamanho no meio da troca.
4. Conferir que marcas e contornos da bancada inativa continuam EXATAMENTE no lugar (em cima das sementes certas) enquanto ela mostra a reduzida — é a regra 2 (geometria) na prática.
5. No gerenciador de tarefas do sistema (ou `about:memory`/`chrome://memory` do navegador), comparar o uso de memória da aba com as duas bancadas ativas (cheias) contra as duas inativas (uma reduzida) — deve haver queda visível com imagens de dezenas de MB cada.
6. Trocar de bancada BEM rápido, várias vezes seguidas (mais rápido do que a imagem consegue decodificar) — não deve travar, não deve mostrar a imagem errada, não deve gerar erro no console além do "não foi possível recarregar" (que só deveria aparecer se o arquivo de fato falhar).
7. Restaurar uma sessão antiga (histórico) numa bancada, depois trocar essa bancada para inativa e de volta — como sessão restaurada não tem `File` de origem, a imagem NUNCA deve ser reduzida (fica cheia o tempo todo, mesmo inativa). Confirma a regra 4.

**O que este agente não conseguiu provar** (sem navegador nesta sessão):

- Os sete passos do roteiro acima, por inteiro.
- Os dois números pedidos pelo plano (tempo de reduzir, tempo de recarregar) — ver a seção de medição acima para o roteiro exato de quem for medir.
- Que a memória de fato cai na prática (a garantia aqui é de código — nenhuma referência à `HTMLImageElement` cheia sobrevive depois de `liberarImagemCheia` aplicar a troca —, não uma medição com profiler).
- O comportamento com sessão restaurada (passo 7) é o único ponto desta task com um caminho de código genuinamente diferente (sem `File`); não foi reproduzido, só verificado por leitura de `App.handleLoadSession` e do `arquivoDeOrigemAtual` que o consome.

---

## Task 5 — feita (17/09)

987 testes (971 na baseline desta sessão, já incluindo trabalho paralelo de outra task + 16 novos em `comparacao.test.ts`), `tsc --noEmit` limpo, `eslint src` sem erro em nenhum arquivo tocado por esta task (1 erro pré-existente em `useBancada.ts`, de uma task em andamento em paralelo — ver nota abaixo), `npm run build` ok.

**O que entrou:**

- `src/features/bancadas/comparacao.ts` *(novo, puro)* — `compararBancadas(entradas)` devolve uma `LinhaDeComparacao` por bancada aberta com imagem, reaproveitando `enumerarObjetos` (contagem/pareamento marca↔contorno) e `medianaDaCena` (aglomerado.ts) para a área — nenhum dos dois foi recalculado. `decidirEscala(linhas)` é a regra que não cai: com duas ou mais calibrações presentes, compara a maior contra a menor; acima de 5% de divergência, devolve `usarMm2: false` com uma frase pronta para a interface (a tabela então mostra px², que não depende de calibração nenhuma). `serieNoTempo(linhas, datas)` só declara série quando todas as linhas são a MESMA placa (`trim` + minúsculas) e todas têm data, com pelo menos duas datas distintas — qualquer outro caso devolve `null` sem invalidar a comparação lado a lado.
- `src/features/bancadas/__tests__/comparacao.test.ts` — 16 testes: contagem e área mediana por linha, ausência de mm² sem calibração, o rótulo "D0/D7" só nascendo com placa comum E data em todas, e os dois lados de `decidirEscala` e `serieNoTempo` (concorda → mm², diverge → px² com motivo; mesma placa+datas diferentes → série; placas diferentes, sem placa, data faltando, mesma data, ou uma bancada só → `null`).
- `src/features/bancadas/PainelDeComparacao.tsx` *(novo)* — só desenha algo com duas ou mais bancadas COM IMAGEM (devolve `null` antes disso). Tabela com uma coluna por bancada (linhas: total, viáveis, inviáveis, área mediana, escala), a frase de `decidirEscala` quando ela existir, gráfico de linha (recharts, já no chunk separado por `manualChunks` do `vite.config.ts` — nenhuma mudança de bundling foi necessária) quando há série no tempo, e exportação CSV com `;` e vírgula decimal (mesma convenção de `limites-adaptaveis.ts`/`synthetic-data.ts`; `measurementsToCSV` não foi reaproveitado porque suas colunas são por SEMENTE, não por bancada — reaproveitar teria significado desmontar as duas formas). A cor das linhas viáveis/inviáveis no gráfico usa `--color-ov-viable`/`--color-ov-inviable` (as mesmas do canvas), respeitando a restrição de ciano/magenta só para essa legenda.
- `src/components/layout/RightSidebar.tsx` — ganhou o prop `comparacaoContent?: React.ReactNode`, no mesmo padrão de `datasetsContent`/`loteContent`: o componente não conhece bancada nenhuma, só desenha o que recebe (com um `<hr>` antes, só quando o prop existe).
- `src/App.tsx` — computa `bancadasComImagemParaComparar` (bancadas abertas com `fila.image`) e só monta `<PainelDeComparacao>` quando esse número é ≥ 2; com menos, passa `undefined`. **Por que a contagem mora no App, e não só dentro do painel:** `RightSidebar` decide o separador pela PRESENÇA do prop, e um elemento React é "presente" mesmo quando o componente que ele instancia devolve `null` — se o App sempre passasse `<PainelDeComparacao bancadas={bancadas} />`, o `<hr>` apareceria sobrando com uma bancada só, mesmo a tabela não desenhando nada.

**Decisão que o plano não fixava, e como foi resolvida:** `EntradaDeComparacao` (o parâmetro de `compararBancadas`) não estava no plano — só `LinhaDeComparacao` e as duas assinaturas de função estavam. Foi desenhado com `bancada`, `arquivo`, `marks`, `segmentacoes`, `umPerPixel`, `placa` e `data` opcionais. `LinhaDeComparacao` também ganhou um campo a mais que o plano listava: `placa?: string` (normalizada), comentado como "não é uma coluna da tabela" — é o único jeito de `serieNoTempo` confirmar "mesma placa" recebendo só `linhas` e `datas` como parâmetros (a assinatura prescrita não repete `entradas`).

**Fonte da "data" de cada bancada, que `Metadata` não tem:** o app não pergunta "quando foi tirada esta foto" em lugar nenhum. Na falta de um campo próprio, `PainelDeComparacao` usa `file.lastModified` do arquivo carregado (via `imageQueue[currentImageIndex]`) como data de referência. É uma aproximação, não uma data de avaliação declarada — registrado aqui para quem for construir a Task 6 (persistência) saber que esse buraco existe e decidir se merece um campo próprio em `Metadata` em vez de continuar emprestando o `lastModified` do sistema de arquivos.

**Trabalho em paralelo, não tocado por esta task:** outra sessão mexia em `src/hooks/useBancada.ts` e `src/features/bancadas/Bancadas.tsx` ao mesmo tempo (redução de memória — Task 4). Os dois arquivos ficaram fora do escopo desta task por instrução explícita; o `Bancada` que `PainelDeComparacao` consome (`fila`, `anotacoes`, `meta`) não mudou de forma, só ganhou funcionalidade interna nova. Havia 1 erro de lint (`CanvasImageSource` não definido) em `useBancada.ts` no momento desta verificação — não foi corrigido aqui por não ser meu arquivo; quem fechar a Task 4 precisa resolvê-lo antes do checkpoint final valer para as duas tasks juntas.

### ROTEIRO PARA O ENRICO

1. Abrir uma imagem só (uma bancada) — a aba Resultados deve estar EXATAMENTE como antes: sem tabela de comparação, sem separador extra.
2. Abrir uma segunda bancada e carregar uma imagem diferente nela — a aba Resultados ganha a seção "Comparação entre bancadas", com uma coluna por bancada.
3. Calibrar as duas bancadas com a MESMA régua/DPI (ou deixar sem calibrar) — a linha "Área mediana" deve aparecer em mm² (ou px² se nenhuma calibrada); calibrar uma bancada com um DPI bem diferente da outra (por exemplo 600 vs. 2400 dpi) deve trocar a tabela para px² e mostrar a frase explicando a divergência.
4. Preencher o mesmo texto no campo "Placa" das duas bancadas (com espaços/maiúsculas diferentes, para conferir que a comparação ignora isso) — sem datas artificiais para forçar de verdade, o gráfico de série no tempo provavelmente não aparece nesta sessão (a "data" vem do `lastModified` do arquivo, que normalmente é o mesmo dia); é esperado, não é bug.
5. Clicar "CSV" no painel — o arquivo baixado deve abrir corretamente no Excel (BOM, `;`, vírgula decimal) com uma linha por bancada.
6. Fechar bancadas até sobrar uma — a seção de comparação deve desaparecer, sem deixar separador ou espaço vazio para trás.

**O que este agente não conseguiu provar** (sem navegador nesta sessão, e sem biblioteca de teste de componente no projeto):

- Todo o roteiro acima — a tabela de fato aparecendo/desaparecendo com o número certo de bancadas, a troca visual para px² com a frase, o gráfico de linha renderizando de verdade, e o CSV abrindo corretamente no Excel.
- Que a série no tempo realmente aparece num caso real: como a "data" vem do `lastModified` do arquivo (não há campo de data explícito em `Metadata`), duas fotos da mesma placa tiradas em dias diferentes e depois transferidas juntas para o computador podem ter o mesmo `lastModified` de cópia — o roteiro acima já avisa disso no passo 4, mas só um teste com arquivos de fato datados diferente prova o caminho feliz.
- A leitura visual das cores do gráfico (`--color-ov-viable`/`--color-ov-inviable`) nos dois temas (claro/escuro).
