**Title:** 🧪 Add tests for arcsinTransform and transformPercentages

**Description:**
* 🎯 **What:** Implemented a new test suite and added coverage for the mathematical transformations (`arcsinTransform` and `transformPercentages`) in `src/lib/stats.ts` to ensure precision and reliable bounds checking.
* 📊 **Coverage:** Scenarios covered include basic calculation mapping, empty array handling, and bounding/clamping limits (input less than 0 and greater than 100).
* ✨ **Result:** The project now has `vitest` correctly configured as its test runner and 9 fully passing test cases for the statistical data transformations, preventing regressions for subsequent ANOVA-related edits.
